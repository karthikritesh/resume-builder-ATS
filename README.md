# resume-builder-ATS
This repo consists of a resume builder which is ATS friendly, you can run the github action available to build your resume and download it as artifact

# Alternately, download the .py file and run it in your local
```python
pip install reportlab
python resume.py
```
It will generated resume.pdf
